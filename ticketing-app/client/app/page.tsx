const LandingPage = async () => {
  // const fetch = buildFetch(headers());
  // let res = await (await fetch("/api/users/currentuser")).json();
  // console.log(res);
  // return <div className='p-6 max-w-sm mx-auto bg-white rounded-xl shadow-md flex items-center space-x-4'>Headers: {JSON.stringify(res)}</div>;
  return <h1>Landing Page</h1>;
};

export default LandingPage;
